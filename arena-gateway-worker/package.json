{
  "name": "arena-gateway-worker",
  "version": "0.1.0",
  "description": "Arena $HERO payment gateway as a Cloudflare Worker (KV-backed, self-settled on Robinhood Chain)",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run --outdir /tmp/arena-worker-build"
  },
  "dependencies": {
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "wrangler": "^4.83.0"
  }
}
